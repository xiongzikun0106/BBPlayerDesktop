# 阶段 6 进展记录

> 施工单与两份安卓端代码研究报告见 [`DESKTOP_UI_PLAN_STAGE6.md`](./DESKTOP_UI_PLAN_STAGE6.md)。
> 本文件只记**做了什么、怎么验证的、还剩什么**。

## 已完成

### 框架与前置（不是界面，但决定界面能不能被验证）

| 项               | 说明                                                                                                                                                                                                                             | 提交       |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| **静默测试**     | 探针窗口 `show:false` + `skipTaskbar:true`，不显示、不抢焦点；`--ui-tour` 补进 `PROBE_ENABLED`（**原本不在**）                                                                                                                   | `e46c5962` |
| **截图像素体检** | 数不同颜色，≤2 种判为空图。DOM 体检**证明不了画出来了** —— 隐藏窗口被节流会截出纯色图而 DOM 全绿。已证明守卫有效（纯色图 1 色判空 / 真实图 73–134 色）                                                                           | `e46c5962` |
| **崩溃不再弹窗** | Electron 对主进程未捕获异常会弹**系统模态框**，会抢焦点。探针模式改为打到 stderr + 非 0 退出                                                                                                                                     | `eaa4f743` |
| **崩溃不再假绿** | `.catch(console.error)` + `.finally(app.exit(0))` 让**驱动崩溃也返回 0** —— 实测一条断言把 API 路径写错抛异常，套件照样报「通过 122 项，失败 0 项」而**后面的断言根本没跑**。影响全部 11 个探针块，已加 `probeFailed` 接到退出码 | `eaa4f743` |

### 6a 歌词匹配（唯一有客观指标的一项）

移植 `lyrics_hit_rate_test_molde` 的 v2 实现进 `packages/core/src/services/lyricMatcher.ts`：

| 策略                            | 端到端命中 | 过度拒绝  | 误自动采用 |
| ------------------------------- | ---------- | --------- | ---------- |
| `core-merged`（改前，冻结基线） | **0.0%**   | **99.0%** | 0.0%       |
| `prod-merged@mobile`（改后）    | **87.0%**  | 0.0%      | 0.0%       |
| `prod-merged@v2`（改后）        | **88.0%**  | 0.0%      | 0.0%       |

改前是"召回 99% 但一律放弃" —— 这就是所有截图里「暂无歌词」的原因。

⚠️ 提交前拦下一个正确性风险：`AUTO_MATCH_THRESHOLD`（0.75→0.45）**被桌面端
外链歌单导入器共用**（`track-matcher.cjs:210`）。两套打分函数不同却共用一个阈值，
已给导入器写回自己的 `0.75/0.45`（行为一字不变）。
⚠️ 顺带修掉 `scripts/verify-lyrics.mts` 一条**失效的代理断言**（49/1 → 50/0）。
提交：`e035063c`

### 6b 结构骨架

量出来的根因：**分割细线从 `.main` 的 x=0 通栏拉过去，而内容是内缩 16px**
→ 线与字永远差 16px。修法不是把线缩进（那还是直角），而是把功能区做成卡片，
对齐问题**从结构上消掉**。去掉了 `.topbar` / `.view-tabs` / `.favorite-bar` /
`.sidebar__brand` 四处的通栏线，`#content` / `#view-settings` / `#view-share`
统一成同一张卡片面（`--surface-container` + `--r-lg`）。

⚠️ 中途发现自己的不一致：只给 `#content` 做卡片时**设置页与共享页漏了** ——
深色下设置页仍是"平铺一大片"，正是用户抱怨的那一坨。三处已统一。

设置列表改为**卡片内按行分隔、分隔线内缩到内容**（用 `::before` 而非
`border-top` —— 后者会横跨整行宽度，又变成通栏线）。
提交：`3c8b2c67` `11bdc2d3`

### 6c 曲目操作与「添加到歌单」

- **操作列两个图标合成一个「⋯」**，点开弹出菜单（下一首播放 / 添加到歌单 /
  从歌单移除）。组件层新增可复用的 `menu()`，与安卓端 `TrackMenuItem` 对齐。
  ⚠️ 关闭时机有三条（选中 / 点外面 / Esc），少一条就关不掉。
- **写库路径** `db.addTracksToPlaylist`：重复**静默忽略**（与移动端
  `onConflictDoNothing` 同语义），并把 `added`/`skipped` 分开报。
  ⚠️ 事务与 DB 细节下沉到 db 层 —— 第一版写在 IPC 里，既编排了不存在的事务
  API，也让第二个调用方将来还得再抄一遍。
- **「添加到歌单」弹层**：居中 Dialog + 单选 + 左侧「创建歌单」。
  ⚠️ **主动修掉移动端的断点**：安卓端从弹层点「创建歌单」成功后 `closeAll()`
  关闭所有弹层，既不把选中的歌加进去也不回到弹层 —— **用户白选一场**。
  桌面端改成内联新建 + 自动选中新歌单 + 保留已选曲目。
  单选标记用 CSS 画圆（`radio_button_*` 不在图标子集里，重建需联网）。

提交：`5da4e3d7` `ebbc59e3`

### 6d 音乐库 › 播放列表 = 歌单卡片网格

| 项                               | 说明                                                                                                                                                                                                                                                                                                               | 提交 |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---- |
| **页签内容换成歌单列表**         | 安卓端这个页签的内容**从来不是曲目表**，而是歌单列表（`LocalPlaylistList.tsx`：头部「播放列表 / N 个播放列表 / ＋菜单」+ 胶囊搜索框 + 歌单行）。桌面端原来渲染的是"当前歌单的曲目表"，歌单列表挂在左栏，**新建入口完全不存在** —— 正是用户说的「应该是呈卡片样式展示歌单列表」「我没有看到任何新建本地歌单的按钮」 | —    |
| **卡片网格（因宽屏改变）**       | 保留：页签内容 = 歌单列表、页内搜索、点卡片进详情。改变：竖排单列 → **卡片网格**（`auto-fill minmax(148px,1fr)`，下界取自安卓端的卡宽 140）。中栏 ~1000px 下单列会每行浪费 600 多像素                                                                                                                              | —    |
| **卡片是同一张卡**               | 新增组件 `mediaCard()`（`components.js`），与安卓端「近期歌单」的卡逐条对齐：圆角、面底色、**1:1 封面**、标题最多 2 行、副标题「N 首」+ 状态图标。音乐库与主页两处**必须是同一张卡**                                                                                                                               | —    |
| **＋菜单 3 项**                  | 安卓端是 4 项（含「动态合并歌单」），桌面端**没有**动态歌单的实现，所以只放 3 项：新建播放列表 / 导入外部歌单 / 订阅共享歌单。后两项**复用已有入口**（导入页签、共享面板），不另起一套                                                                                                                             | —    |
| **详情要有回路**                 | 点卡片进详情（曲目表）后，`.view-head` 里给一条「← 播放列表」。安卓端这是路由自带的返回；桌面端没有导航栈，必须自己给                                                                                                                                                                                              | —    |
| **⚠️ 作者列永远是「—」的真根因** | 与上一轮修的**不是同一条路径**。`getPlaylistTracks` 只 `SELECT t.*`，而 `tracks` 表里存的是外键 `artist_id`，作者名在 `artists.name` —— **数据库路径下作者名根本不在结果集里**。收藏夹预览那条（`upperName`）是接口字段，上一轮修的是它。已加 `LEFT JOIN artists`，实测 **24/24 行**有作者                         | —    |
| 顺带                             | 播放条 / 正在播放面板的作者也要认 `artist_name`；歌词自动匹配原来只传 `track.artist`，从歌单播放时**丢掉歌手证据**（匹配里占 0.3 权重）                                                                                                                                                                            | —    |

**验证**：ui 探针 **131 → 143 项**（卡片网格 / 封面 1:1 / 卡片底色 / ＋菜单三项 / 点卡片进详情 + 返回 / 作者列 / 页内筛选 / **走完整路径新建歌单**），全绿；浅色巡检 34 张 0 问题。

**一条测试自身的坑**：新建歌单那条用例会在库里留下一个**空歌单**，而它可能排在左栏第一位 ——
后面"点左栏第一行 → 点播放全部"的断言就全挂了。已改成**按 `item_count > 0` 挑歌单**
（`openPlaylistWithTracks`）。现象看起来像"功能坏了"，其实是测试挑错了对象。

**另一个只有截图才会发现的坑**：我给非选中的卡片加了底色，但截图**一模一样**（连 PNG 字节都相同）——
因为库里只有一张卡，而它正好是 `is-active`，被高亮色盖住了。
所以断言拆成两条：选中态用高亮色、**未选中**的卡用自己的底色。

### 6e 细节

| 项                        | 根因                                                                                                                                                                                  | 提交                  |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| **所有开关渲染成勾选框**  | CSS **特异性**：`input[type='checkbox']`(0-1-1) 压过 `.switch`(0-1-0)，加载顺序救不了。提到 `input[type='checkbox'].switch`(0-2-1)                                                    | `9a7b73e1`            |
| **胶囊永不消失**          | `status.js` 只让 `ok`/`warn` 淡出，`idle`/`busy`/`bad` 永远挂着。改成每种都淡出并分档（bad/busy 9s、ok/warn 4.5s、idle 3.5s）                                                         | `9a7b73e1` `77a7378d` |
| **登录成功无提示**        | 三条登录路径都只改了弹窗内一行小字。抽出 `finishLogin()`：关弹窗 + 胶囊提示「登录成功，欢迎 XXX」                                                                                     | `6e8354c6`            |
| **关于页太潦草**          | `.settings-diagnostics` **完全没有样式**（裸 `<details>` + 裸 `<dl>`）。改成嵌套卡片 + 两列网格键值行 + 内缩分隔                                                                      | `d9626ae0`            |
| **关于页加 GitHub 按钮**  | 新增 `app:openExternal`，**带白名单**（它会拉起系统浏览器，不能接受任意 URL）                                                                                                         | `d9626ae0`            |
| **用户名放到品牌名右边**  | 用户明确要求。未登录时隐藏而不是写"未登录"（品牌行只有 240px）。顺手补掉 `.sidebar__brand` 上 6b 漏改的通栏线                                                                         | `6731b76d`            |
| **收藏夹作者列永远「—」** | 用真实接口排除了"接口不给作者"（10/10 条都带 `upper`）。真因是**两套渲染器认两套字段名**：`library.js` 只读 `artist`/`artist_name`，而接口来的是 `upperName`                          | `e035063c`            |
| **自绘取色控件**          | `input[type="color"]` 点开弹的是**操作系统**的取色器 —— OS 窗口 CSS 碰不到，"圆角/主题色"从原理上做不到。改成自绘：预设色块 + 十六进制输入，**不弹浮层**                              | `b89a14d3`            |
| **顶部菜单改播放菜单**    | 原为 Electron 默认（全仓库无 `setApplicationMenu`）。现在是 `[播放 / 前往 / 窗口 / 帮助]`。⚠️ **不新写一套分发**：菜单只发组合键，由 `bbKeys.trigger(combo)` 转交给**已注册的**处理器 | `01069eab` `a9f43c18` |

### 阶段 6 里最有价值的三条教训

1. **`accelerator` 会吃掉渲染进程的快捷键**。菜单项设了 `accelerator` 之后，
   Electron 在**菜单层**拦截 `Space`/`Left`/`Right`/`Ctrl+Q`，
   `keys.register` 的处理器收不到 keydown —— 一次挂掉 4 条键盘断言。
   渲染进程本来就有完整的快捷键体系，菜单不该再抢一遍；
   快捷键改为写进**标签文字**。

2. **测试之间会相互污染，而断言本身看不出来**。上面那 4 条在去掉
   `accelerator` 后**仍然失败** —— 真因是**我的新断言自己**：它用
   「前往 › 搜索」验证桥，切到搜索页后**焦点落在搜索框**，
   于是 `Space` 变成打空格、`←` 变成移动光标。
   最终改用「播放队列 / 歌词」（不换视图、不动焦点）。
   **三次尝试，每一次的失败原因都不是我以为的那个。**

3. **CSS 特异性在这个项目里咬了三次**：开关（`.switch` vs
   `input[type=checkbox]`）、取色色块（`.swatch` vs `.settings-row button`）。
   凡"样式明明写了却没生效"，**先怀疑特异性，再怀疑加载顺序**。

### 断言的写法（反复踩过，已成规矩）

- **只断言"元素存在"是不够的** —— 点不开的按钮等于坏按钮，要走真实的打开路径；
- **描述字符串里的检查不是检查** —— 登录那条漏把文案判断写进**条件**，
  于是任何 `ok` 胶囊都能通过（假绿）；
- **读数据源，不要猜选择器** —— 搜索空态那次查了一个**不存在**的属性，
  恒为 0 → 永远报"已覆盖"；
- **代理断言会失效** —— `verify-lyrics` 用打分间接验证归一化，
  实现换算法后代理失效（改成直接断言 `normalizeTitle`）；
- **喂给驱动的脚本要短而平** —— 在 `evaluate` 的模板里塞三元 +
  多行链式调用会让渲染进程解析失败，而**那种报错不给行号**。

## 尚未完成

| 项                                  | 说明                                                                                                                                                                                      |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **6d 主页重做 + 近期歌单 + 热力图** | 最大的一块。⚠️ 安卓端的「近期歌单」实际查的是 `ORDER BY updatedAt DESC`，而播放**不写** `updatedAt` —— 它是「最近**修改**过的歌单」，名字名不副实。计划先按「最近更新」这个诚实的措辞实现 |
| 多选                                | 「添加到歌单」已可用，多选是它的批量入口。⚠️ 选中键**必须用 `uniqueKey` 而非 `track.id`** —— 同一个 id 在搜索结果里是 `aid`、收藏夹是 `bv2av(bvid)`、分P 是 `cid`，跨列表会串味           |
| 桌面歌词                            | 用户明确要求放到最后（本 goal 不做）                                                                                                                                                      |

## 已知的环境性失败（不是回归）

`verify:desktop` 里依赖**真实 B 站音频**的用例会偶发失败：

- 出现过 `Range 请求返回 502`（上游暂时性故障，重跑即恢复）；
- `seek 生效` 偶发失败（实测 `2.39s → 30.00s`），属真实音频的时长/边界问题。

这两类都**不经过本次改动的任何代码路径**（音频走 `audio-proxy.cjs`，
阶段 6 改的是渲染层与 db 层），且在**同一会话中同一套件曾多次 18/18 通过**。
